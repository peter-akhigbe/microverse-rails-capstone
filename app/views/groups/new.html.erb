<%= render "shared/header", title: 'New Groups', navPath: groups_path  %>
<%= render "form", group: @group, icons: @icons, class: 'form' %>

<%#= render "shared/footer", hasButton: false %>

<style>
    body {
        display: flex;
        flex-direction: column;
        align-items: center;
    }
</style>